const theme = {
  layout: 2, // either 1 or 2; 1 is HEADER first, 2 is SIDEBAR First
  sidebarWith: 220,
  CONST: {
    THEME: {
      LAYOUT: {
        HEADER_FIRST: 1,
        SIDEBAR_FIRST: 2
      }
    }
  }
};

export default theme;
